Record intent before an infrastructure operation. Provide the tool name,
operation, and raw artifact (manifest, plan, etc.). Returns risk assessment,
prescription ID, and artifact digest. Always call before kubectl apply,
terraform apply, helm upgrade, or any infrastructure mutation.
