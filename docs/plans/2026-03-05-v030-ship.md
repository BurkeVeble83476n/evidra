# v0.3.0 — Ship Release

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Downgrade to Go 1.23, run MCP inspector E2E tests, set up CI/release pipeline with Homebrew, and tag v0.3.0.

**Architecture:** No new features. This plan is Go version downgrade, E2E verification, CI/CD pipeline, and release packaging.

**Tech Stack:** Go 1.23, GoReleaser, GitHub Actions, `samebits/homebrew-tap`.

**Current state:** 219 tests passing, race-clean, gofmt-clean, zero TODOs. All P0/P1 E2E gaps fixed. MCP inspector test suite exists (`tests/inspector/`) with 6 JSON cases + 3 special tests.

---

## Phase 1: Go Version Downgrade

### Task 1: Downgrade Go to 1.23

Go 1.23 is the minimum viable version — the MCP SDK (`go-sdk v1.3.1`) requires Go 1.23.0. Go 1.24 has no features used by our code. Downgrading improves adoption for teams on LTS Go versions.

**Files:**
- Modify: `go.mod`

**Step 1: Change Go version in go.mod**

Change line 3 from `go 1.24.6` to `go 1.23.0`. Remove any `toolchain` directive if present.

**Step 2: Tidy and verify**

```bash
go mod tidy
go build ./...
go test ./... -count=1
go test -race ./...
```

All 219+ tests must pass. No code changes needed — the codebase uses no Go 1.24-specific features.

**Step 3: gofmt, commit**

```bash
gofmt -w .
git add go.mod go.sum
git commit -m "build: downgrade to Go 1.23 for broader adoption"
```

---

## Phase 2: MCP Inspector E2E Tests

### Task 2: Run existing MCP inspector tests

The test suite at `tests/inspector/` uses the MCP Inspector CLI (`npx @modelcontextprotocol/inspector`) to exercise prescribe/report/get_event over real stdio transport. Six JSON-driven cases plus three special shell tests exist.

**Prerequisites:** `npx` (Node.js) and `jq` must be available.

**Step 1: Build the binary**

```bash
make build
```

**Step 2: Run inspector tests**

```bash
make test-mcp-inspector
```

This runs `tests/inspector/run_inspector_tests.sh` in `local-mcp` mode, which:
1. Builds `bin/evidra-mcp`
2. Resets evidence directory (`/tmp/evidra-inspector-evidence`)
3. Runs special tests: `t_list_tools.sh`, `t_get_event_chain.sh`, `t_schema_error.sh`
4. Runs JSON cases: `lifecycle_ok`, `risk_tag_privileged`, `parse_error_artifact`, `report_unknown_prescription`, `cross_actor_report`, `schema_error_prescribe`
5. Asserts on each response using `jq`-based matchers

**Step 3: Verify all pass**

Expected output:
```
MCP Inspector E2E Tests
Mode: local-mcp

-- Special Cases --
  ✓ list_tools
  ✓ get_event_chain
  ✓ schema_error

-- Curated Cases --
  ✓ lifecycle_ok
  ✓ risk_tag_privileged
  ✓ parse_error_artifact
  ✓ report_unknown_prescription
  ✓ cross_actor_report
  ✓ schema_error_prescribe

-- Summary --
  Mode:    local-mcp
  Passed:  9
  Failed:  0
  Skipped: 0

All tests passed.
```

If any test fails, fix the issue and re-run. Do not proceed until all pass.

**Step 4: No commit needed (tests are read-only verification)**

---

### Task 3: Add Go-native MCP transport test

The inspector tests require `npx` (Node.js). Add a pure Go E2E test using the SDK's `NewInMemoryTransports()` so CI can run MCP E2E without Node.js.

**Files:**
- Create: `pkg/mcpserver/e2e_test.go`

**Step 1: Write the E2E transport test**

```go
package mcpserver

import (
	"context"
	"encoding/json"
	"testing"
	"time"

	"github.com/modelcontextprotocol/go-sdk/mcp"
)

func TestE2E_PrescribeReportLifecycle(t *testing.T) {
	t.Parallel()
	dir := t.TempDir()

	server := NewServer(Options{
		Name:         "test",
		Version:      "0.0.1",
		EvidencePath: dir,
		Environment:  "test",
	})

	ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
	defer cancel()

	st, ct := mcp.NewInMemoryTransports()
	serverSession, err := server.Connect(ctx, st, nil)
	if err != nil {
		t.Fatalf("server connect: %v", err)
	}
	defer serverSession.Wait()

	client := mcp.NewClient(
		&mcp.Implementation{Name: "test-client", Version: "0.0.1"},
		nil,
	)
	session, err := client.Connect(ctx, ct, nil)
	if err != nil {
		t.Fatalf("client connect: %v", err)
	}
	defer session.Close()

	// List tools — verify all 3 registered
	tools, err := session.ListTools(ctx, nil)
	if err != nil {
		t.Fatalf("list tools: %v", err)
	}
	toolNames := make(map[string]bool)
	for _, tool := range tools.Tools {
		toolNames[tool.Name] = true
	}
	for _, name := range []string{"prescribe", "report", "get_event"} {
		if !toolNames[name] {
			t.Errorf("missing tool %q in tools/list response", name)
		}
	}

	// Prescribe
	prescribeResult, err := session.CallTool(ctx, &mcp.CallToolParams{
		Name: "prescribe",
		Arguments: map[string]any{
			"actor": map[string]any{
				"type":   "agent",
				"id":     "test-agent",
				"origin": "e2e-test",
			},
			"tool":         "kubectl",
			"operation":    "apply",
			"raw_artifact": "apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: nginx\n  namespace: default\nspec:\n  replicas: 1\n  selector:\n    matchLabels:\n      app: nginx\n  template:\n    metadata:\n      labels:\n        app: nginx\n    spec:\n      containers:\n      - name: nginx\n        image: nginx:1.21\n",
		},
	})
	if err != nil {
		t.Fatalf("prescribe: %v", err)
	}

	var prescribeOut PrescribeOutput
	if err := extractStructuredContent(prescribeResult, &prescribeOut); err != nil {
		t.Fatalf("parse prescribe output: %v", err)
	}
	if !prescribeOut.OK {
		t.Fatalf("prescribe not ok: %+v", prescribeOut)
	}
	if prescribeOut.PrescriptionID == "" {
		t.Fatal("prescribe returned empty prescription_id")
	}
	if prescribeOut.RiskLevel == "" {
		t.Fatal("prescribe returned empty risk_level")
	}
	if prescribeOut.ArtifactDigest == "" {
		t.Fatal("prescribe returned empty artifact_digest")
	}

	// Report
	reportResult, err := session.CallTool(ctx, &mcp.CallToolParams{
		Name: "report",
		Arguments: map[string]any{
			"prescription_id": prescribeOut.PrescriptionID,
			"exit_code":       0,
		},
	})
	if err != nil {
		t.Fatalf("report: %v", err)
	}

	var reportOut ReportOutput
	if err := extractStructuredContent(reportResult, &reportOut); err != nil {
		t.Fatalf("parse report output: %v", err)
	}
	if !reportOut.OK {
		t.Fatalf("report not ok: %+v", reportOut)
	}
	if reportOut.ReportID == "" {
		t.Fatal("report returned empty report_id")
	}

	// Get event — retrieve the prescription entry
	getEventResult, err := session.CallTool(ctx, &mcp.CallToolParams{
		Name: "get_event",
		Arguments: map[string]any{
			"event_id": prescribeOut.PrescriptionID,
		},
	})
	if err != nil {
		t.Fatalf("get_event: %v", err)
	}

	var getEventOut GetEventOutput
	if err := extractStructuredContent(getEventResult, &getEventOut); err != nil {
		t.Fatalf("parse get_event output: %v", err)
	}
	if !getEventOut.OK {
		t.Fatalf("get_event not ok: %+v", getEventOut)
	}
	if getEventOut.Entry == nil {
		t.Fatal("get_event returned nil entry")
	}
	if getEventOut.Entry.EntryID != prescribeOut.PrescriptionID {
		t.Errorf("get_event entry_id mismatch: got %q, want %q",
			getEventOut.Entry.EntryID, prescribeOut.PrescriptionID)
	}
}

func TestE2E_UnprescribedReport(t *testing.T) {
	t.Parallel()
	dir := t.TempDir()

	server := NewServer(Options{
		Name:         "test",
		Version:      "0.0.1",
		EvidencePath: dir,
	})

	ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
	defer cancel()

	st, ct := mcp.NewInMemoryTransports()
	if _, err := server.Connect(ctx, st, nil); err != nil {
		t.Fatalf("server connect: %v", err)
	}

	client := mcp.NewClient(
		&mcp.Implementation{Name: "test-client", Version: "0.0.1"},
		nil,
	)
	session, err := client.Connect(ctx, ct, nil)
	if err != nil {
		t.Fatalf("client connect: %v", err)
	}
	defer session.Close()

	// Report with unknown prescription
	result, err := session.CallTool(ctx, &mcp.CallToolParams{
		Name: "report",
		Arguments: map[string]any{
			"prescription_id": "NONEXISTENT",
			"exit_code":       1,
		},
	})
	if err != nil {
		t.Fatalf("report: %v", err)
	}

	var out ReportOutput
	if err := extractStructuredContent(result, &out); err != nil {
		t.Fatalf("parse output: %v", err)
	}
	if out.OK {
		t.Error("report with unknown prescription should not be ok")
	}
	if out.Error == nil || out.Error.Code != "prescription_not_found" {
		t.Errorf("expected error code 'prescription_not_found', got %+v", out.Error)
	}
}

func TestE2E_ListResources(t *testing.T) {
	t.Parallel()
	dir := t.TempDir()

	server := NewServer(Options{
		Name:         "test",
		Version:      "0.0.1",
		EvidencePath: dir,
	})

	ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
	defer cancel()

	st, ct := mcp.NewInMemoryTransports()
	if _, err := server.Connect(ctx, st, nil); err != nil {
		t.Fatalf("server connect: %v", err)
	}

	client := mcp.NewClient(
		&mcp.Implementation{Name: "test-client", Version: "0.0.1"},
		nil,
	)
	session, err := client.Connect(ctx, ct, nil)
	if err != nil {
		t.Fatalf("client connect: %v", err)
	}
	defer session.Close()

	// List resources
	resources, err := session.ListResources(ctx, nil)
	if err != nil {
		t.Fatalf("list resources: %v", err)
	}
	found := false
	for _, r := range resources.Resources {
		if r.Name == "evidra-evidence-manifest" {
			found = true
			break
		}
	}
	if !found {
		t.Error("expected evidra-evidence-manifest in resource list")
	}
}

// extractStructuredContent parses the structured content from a CallToolResult.
func extractStructuredContent(result *mcp.CallToolResult, v any) error {
	if result.StructuredContent != nil {
		return json.Unmarshal(result.StructuredContent, v)
	}
	// Fallback: parse from text content
	if len(result.Content) > 0 {
		if tc, ok := result.Content[0].(mcp.TextContent); ok {
			return json.Unmarshal([]byte(tc.Text), v)
		}
	}
	return json.Unmarshal([]byte("{}"), v)
}
```

**Step 2: Run the test**

```bash
gofmt -w pkg/mcpserver/e2e_test.go
go test ./pkg/mcpserver/ -run TestE2E -v -count=1
```

All 3 E2E tests must pass.

**Step 3: Run full suite to ensure no regression**

```bash
go test ./... -count=1
```

**Step 4: Commit**

```bash
git add pkg/mcpserver/e2e_test.go
git commit -m "test: add Go-native MCP E2E tests via in-memory transport"
```

---

## Phase 3: CI/CD Pipeline

### Task 4: Create CI workflow

**Files:**
- Create: `.github/workflows/ci.yml`

**Step 1: Create the CI workflow**

Adapted from `../evidra-mcp/.github/workflows/ci.yml` — removed OPA steps, updated for benchmark.

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Check formatting
        run: |
          files=$(gofmt -l .)
          if [ -n "$files" ]; then
            echo "The following files are not gofmt-formatted:" >&2
            echo "$files" >&2
            exit 1
          fi

      - name: Run tests
        run: go test ./... -count=1

      - name: Run race detector
        run: go test -race ./... -count=1

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: v1.64
          args: --timeout=5m

      - name: Build binaries
        run: |
          go build -o bin/evidra ./cmd/evidra
          go build -o bin/evidra-mcp ./cmd/evidra-mcp
```

**Step 2: Commit**

```bash
mkdir -p .github/workflows
git add .github/workflows/ci.yml
git commit -m "ci: add GitHub Actions CI workflow"
```

---

### Task 5: Create GoReleaser config

**Files:**
- Create: `.goreleaser.yaml`

**Step 1: Create `.goreleaser.yaml`**

Adapted from `../evidra-mcp/.goreleaser.yaml` — two binaries (no evidra-api yet), updated module path and descriptions.

```yaml
version: 2

project_name: evidra-benchmark

before:
  hooks:
    - go mod tidy

builds:
  - id: evidra
    main: ./cmd/evidra
    binary: evidra
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X samebits.com/evidra-benchmark/pkg/version.Version={{.Version}}
      - -X samebits.com/evidra-benchmark/pkg/version.Commit={{.Commit}}
      - -X samebits.com/evidra-benchmark/pkg/version.Date={{.Date}}

  - id: evidra-mcp
    main: ./cmd/evidra-mcp
    binary: evidra-mcp
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X samebits.com/evidra-benchmark/pkg/version.Version={{.Version}}
      - -X samebits.com/evidra-benchmark/pkg/version.Commit={{.Commit}}
      - -X samebits.com/evidra-benchmark/pkg/version.Date={{.Date}}

archives:
  - id: evidra
    builds:
      - evidra
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "evidra_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

  - id: evidra-mcp
    builds:
      - evidra-mcp
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "evidra-mcp_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

release:
  mode: replace

checksum:
  name_template: checksums.txt

changelog:
  use: git
  sort: asc

brews:
  - name: evidra-mcp
    ids:
      - evidra-mcp
    skip_upload: false
    repository:
      owner: samebits
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://github.com/vitas/evidra-benchmark"
    description: "Flight recorder and reliability benchmark for infrastructure automation."
    license: "Apache-2.0"
    install: |
      bin.install "evidra-mcp"
    test: |
      system bin/"evidra-mcp", "--version"

  - name: evidra
    ids:
      - evidra
    skip_upload: false
    repository:
      owner: samebits
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://github.com/vitas/evidra-benchmark"
    description: "CLI for Evidra reliability benchmark — scorecard, compare, prescribe, report."
    license: "Apache-2.0"
    install: |
      bin.install "evidra"
    test: |
      system bin/"evidra", "--version"
```

**Step 2: Commit**

```bash
git add .goreleaser.yaml
git commit -m "build: add GoReleaser config with Homebrew tap"
```

---

### Task 6: Create release workflow

**Files:**
- Create: `.github/workflows/release.yml`

**Step 1: Create the release workflow**

Adapted from `../evidra-mcp/.github/workflows/release.yml` — removed OPA, docker-api, docker-hosted jobs. Kept: release-guard, test, snapshot, docker (MCP only), goreleaser.

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release-guard:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate tag and version alignment
        run: |
          set -euo pipefail

          if [[ "${GITHUB_REF:-}" != refs/tags/* ]]; then
            echo "::error::Release workflow must run from a tag ref."
            exit 1
          fi

          tag="${GITHUB_REF#refs/tags/}"
          if ! [[ "${tag}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "::error::Invalid tag '${tag}'. Expected semver like v0.3.0."
            exit 1
          fi

          tag_sha="$(git rev-list -n1 "${tag}")"
          if [ "${tag_sha}" != "${GITHUB_SHA}" ]; then
            echo "::error::Tag ${tag} points to ${tag_sha}, but workflow SHA is ${GITHUB_SHA}."
            exit 1
          fi

          code_version="$(sed -nE 's/^[[:space:]]*Version[[:space:]]*=[[:space:]]*"([^"]+)".*/\1/p' pkg/version/version.go | head -n1)"
          if [ -z "${code_version}" ]; then
            echo "::error::Could not parse Version from pkg/version/version.go."
            exit 1
          fi

          tag_mm="$(echo "${tag#v}" | cut -d. -f1-2)"
          code_mm="$(echo "${code_version}" | sed -E 's/^([0-9]+)\.([0-9]+).*/\1.\2/')"

          if [ "${code_mm}" != "${tag_mm}" ]; then
            echo "::error::Tag ${tag} is ${tag_mm}, but pkg/version is ${code_version}."
            exit 1
          fi

          echo "Release guard passed for ${tag} (code=${code_version})."

  test:
    needs: [release-guard]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Check formatting
        run: |
          files=$(gofmt -l .)
          if [ -n "$files" ]; then
            echo "Unformatted files:" >&2
            echo "$files" >&2
            exit 1
          fi

      - name: Run tests
        run: go test ./... -count=1

      - name: Race detector
        run: go test -race ./... -count=1

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: v1.64
          args: --timeout=5m

  snapshot:
    needs: [test]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: GoReleaser snapshot
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: latest
          args: release --snapshot --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  docker:
    needs: [test]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/vitas/evidra-mcp
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: evidra-mcp:smoke

      - name: Smoke test
        run: |
          docker run --rm evidra-mcp:smoke --version \
            || { echo "FAIL: --version failed"; exit 1; }
          echo "PASS: smoke test ok"

      - name: Push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  goreleaser:
    needs: [snapshot]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Verify Homebrew tap token
        env:
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${HOMEBREW_TAP_TOKEN:-}" ]; then
            echo "::error::Missing HOMEBREW_TAP_TOKEN secret."
            exit 1
          fi
          repo_code=$(curl -sS -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer ${HOMEBREW_TAP_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/samebits/homebrew-tap")
          if [ "${repo_code}" != "200" ]; then
            echo "::error::HOMEBREW_TAP_TOKEN cannot access samebits/homebrew-tap (HTTP ${repo_code})."
            exit 1
          fi
          echo "Homebrew tap token verified."

      - name: Reset existing release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          tag="${GITHUB_REF#refs/tags/}"
          if gh release view "${tag}" -R "${GITHUB_REPOSITORY}" >/dev/null 2>&1; then
            echo "Deleting existing release for ${tag}."
            gh release delete "${tag}" -R "${GITHUB_REPOSITORY}" --yes
          fi

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
```

**Step 2: Commit**

```bash
git add .github/workflows/release.yml
git commit -m "ci: add release workflow with GoReleaser and Homebrew"
```

---

### Task 7: Add Dependabot config

**Files:**
- Create: `.github/dependabot.yml`

**Step 1: Create dependabot config**

```yaml
version: 2
updates:
  - package-ecosystem: gomod
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 10

  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
```

**Step 2: Commit**

```bash
git add .github/dependabot.yml
git commit -m "ci: add Dependabot for Go modules and GitHub Actions"
```

---

## Phase 4: README and Release Files

### Task 8: Add CI badges and update README

**Files:**
- Modify: `README.md`

**Step 1: Add badges at top of README**

Insert after line 1 (`# Evidra Benchmark`):

```markdown
[![CI](https://github.com/vitas/evidra-benchmark/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/vitas/evidra-benchmark/actions/workflows/ci.yml)
[![Release](https://github.com/vitas/evidra-benchmark/actions/workflows/release.yml/badge.svg)](https://github.com/vitas/evidra-benchmark/actions/workflows/release.yml)
[![Latest Release](https://img.shields.io/github/v/release/vitas/evidra-benchmark)](https://github.com/vitas/evidra-benchmark/releases/latest)
[![Go Report Card](https://goreportcard.com/badge/samebits.com/evidra-benchmark)](https://goreportcard.com/report/samebits.com/evidra-benchmark)
[![License](https://img.shields.io/badge/License-Apache_2.0-blue.svg)](https://opensource.org/licenses/Apache-2.0)
```

**Step 2: Add Install section after Quick Start**

Insert before `### MCP Server`:

```markdown
### Install

```bash
# Homebrew (macOS / Linux)
brew install samebits/tap/evidra-mcp
brew install samebits/tap/evidra

# Or download from GitHub Releases
# https://github.com/vitas/evidra-benchmark/releases/latest
```
```

**Step 3: Commit**

```bash
git add README.md
git commit -m "docs: add CI badges and install instructions"
```

---

### Task 9: Create CHANGELOG.md

**Files:**
- Create: `CHANGELOG.md`

**Step 1: Create the changelog**

```markdown
# Changelog

## v0.3.0 — 2026-03-05

First public release of Evidra Benchmark.

### Core Pipeline
- Canonicalization adapters: Kubernetes (kubectl, oc, helm), Terraform, generic fallback
- Risk matrix (operation_class x scope_class) with 7 catastrophic detectors
- Five behavioral signals: protocol violation, artifact drift, retry loop, blast radius, new scope
- Weighted reliability scoring with safety floors and band classification

### CLI (`evidra`)
- `prescribe` — record intent before infrastructure operations
- `report` — record outcome after execution
- `scorecard` — compute reliability score from evidence chain
- `explain` — detailed signal breakdown with sub-signals
- `compare` — side-by-side actor comparison with workload overlap
- `--scanner-report` flag for SARIF ingestion (Trivy, Kubescape)
- `--canonical-action` flag for pre-canonicalized actions
- Tool and scope filtering on scorecard/explain/compare

### MCP Server (`evidra-mcp`)
- Stdio transport for AI agent integration
- Tools: prescribe, report, get_event
- Per-operation trace IDs for correlation
- Optional retry loop tracking

### Evidence Chain
- Append-only JSONL with hash-linked entries
- Segmented storage with automatic rotation (5MB default)
- File-based locking for concurrent access

### Build
- Go 1.23 minimum (broad adoption)
- Cross-platform binaries via GoReleaser (linux/darwin/windows, amd64/arm64)
- Homebrew: `brew install samebits/tap/evidra-mcp`
- Docker: `ghcr.io/vitas/evidra-mcp:0.3.0`

### Known Limitations
- ArgoCD uses generic adapter (no Argo-specific metadata)
- MinOperations=100 required for scoring (low-volume actors get `insufficient_data`)
- Signing implemented but not wired into pipeline (v0.4.0)
- No centralized API server (v0.5.0)
```

**Step 2: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs: add CHANGELOG.md for v0.3.0"
```

---

## Phase 5: Version Bump and Tag

### Task 10: Update version string

**Files:**
- Modify: `pkg/version/version.go`

**Step 1: Change version**

Change `Version = "0.3.0-dev"` to `Version = "0.3.0"`.

**Step 2: Build and verify**

```bash
go build -o bin/evidra ./cmd/evidra
go build -o bin/evidra-mcp ./cmd/evidra-mcp
bin/evidra --version
bin/evidra-mcp --version
```

Both should output `0.3.0`.

**Step 3: Commit**

```bash
gofmt -w pkg/version/version.go
git add pkg/version/version.go
git commit -m "release: set version to 0.3.0"
```

---

### Task 11: Final verification

**Step 1: Full test suite**

```bash
go test ./... -v -count=1
```

All 222+ tests must pass (219 existing + 3 new E2E).

**Step 2: Race detector**

```bash
go test -race ./... -count=1
```

**Step 3: Format check**

```bash
gofmt -l .
```

Must produce no output.

**Step 4: Build binaries**

```bash
go build -o bin/evidra ./cmd/evidra
go build -o bin/evidra-mcp ./cmd/evidra-mcp
```

**Step 5: Docker build**

```bash
docker build -f Dockerfile -t evidra-mcp:0.3.0 .
docker build -f Dockerfile.cli -t evidra-cli:0.3.0 .
docker run --rm evidra-mcp:0.3.0 --version
docker run --rm evidra-cli:0.3.0 --version
```

**Step 6: GoReleaser dry run**

```bash
goreleaser release --snapshot --clean
```

If goreleaser is not installed locally, skip — the CI workflow validates this.

**Step 7: MCP inspector tests (if npx available)**

```bash
make test-mcp-inspector
```

---

### Task 12: Tag release

**Step 1: Verify clean working tree**

```bash
git status
git log --oneline -5
```

**Step 2: Tag**

```bash
git tag -a v0.3.0 -m "v0.3.0: first public release of Evidra Benchmark"
```

**Do NOT push** — present the tag to the user for review first. After approval:

```bash
git push origin main
git push origin v0.3.0
```

---

## Secrets Required (one-time setup before first release)

| Secret | Repository Setting | Purpose |
|--------|-------------------|---------|
| `HOMEBREW_TAP_TOKEN` | `vitas/evidra-benchmark` → Settings → Secrets | PAT with Contents: read+write on `samebits/homebrew-tap` |

`GITHUB_TOKEN` is provided automatically by GitHub Actions.

---

## Summary

| Phase | Tasks | What |
|-------|-------|------|
| 1. Go Downgrade | 1 | `go 1.24.6` → `go 1.23.0` |
| 2. E2E Tests | 2-3 | Run inspector tests + add Go-native MCP transport tests |
| 3. CI/CD | 4-7 | CI workflow, GoReleaser, release workflow, Dependabot |
| 4. Docs | 8-9 | CI badges, install instructions, CHANGELOG |
| 5. Release | 10-12 | Version bump, final verification, tag |
